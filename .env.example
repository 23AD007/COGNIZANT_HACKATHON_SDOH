# Production PostgreSQL (choose this form or the DATABASE_URL form below)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=healthlens
DB_USER=healthlens_user
DB_PASSWORD=replace_me

# DATABASE_URL=postgresql+psycopg2://healthlens_user:replace_me@localhost:5432/healthlens

# Local development only; enables data/processed/healthlens_dev.sqlite.
FLASK_ENV=development
# TESTING=false
